var fs = require("fs");
/*
简单写入
    fs.writeFile(file, data[, options], callback)
    fs.writeFileSync(file, data[, options])
       - 参数
        file 要写入的文件的路径
        data  要写入的文件的数据（String Buffer）
        options
 */
var a = 1;
console.log(a);
document.getElementById('')